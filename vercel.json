{
  "buildCommand": "if [ -d \"apps/web\" ]; then npm --prefix apps/web run build; else npm run build; fi",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
